function mostrar() {
	
	/*let numero = 0;
	let contador = 0;
	let acumulador=0;

	while (acumulador < 1000) {
		numero= parseInt(prompt("ingrese un numero"));
		while (isNaN(numero)) {
			numero= parseInt(prompt("ingrese un numero"));
			
		}
		
		acumulador= acumulador + numero;
		contador++;
		
	}
	alert(`la suma es ${acumulador} y se pidieron ${contador} numeros`);*/





	for (let control = 1; control <= 10; control++) {
		console.log(control);


	}
}